import 'package:flutter/material.dart';
class MovieListStatic extends StatelessWidget {
  const MovieListStatic({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(""),
    );
  }
}