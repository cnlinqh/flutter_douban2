import 'package:flutter/material.dart';
class MovieSubjectDetails extends StatelessWidget {
  const MovieSubjectDetails({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(""),
    );
  }
}